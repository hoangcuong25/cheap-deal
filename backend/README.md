# Install miniconda at
https://docs.anaconda.com/miniconda/install/ choose cmd prompt or powershell


# Create enviroment by using 
```(bash)
conda create -n env python=3.11 anaconda
conda init
conda activate env
```

# Install package in requirement
```
pip install -r requirement.txt
```